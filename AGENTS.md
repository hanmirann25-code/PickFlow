# PickFlow — 에이전트 작업 규칙

이 파일은 이 저장소에서 작업하는 모든 AI 코딩 도구의 공통 규칙이다.
상세 배경은 `docs/기획서.md`를 참조할 것. 이 파일에는 매번 지켜야 하는 것만 적는다.

---

## 프로젝트

주문 접수부터 출고까지를 관리하는 풀필먼트 운영 콘솔.
웹 관리자 화면 + 창고 현장용 모바일 화면으로 구성된다.

**이 프로젝트의 목적은 기능 개수가 아니라 아래 다섯 가지를 증명하는 것이다.**

1. 10만 건 규모에서 끊기지 않는 목록 화면
2. 동시 요청에도 재고가 깨지지 않는 처리
3. 서버가 밀어주는 실시간 화면
4. 네트워크가 끊겨도 이어지는 현장 작업
5. 역할별 권한과 변경 이력

**기능 추가를 제안하기 전에 위 다섯 가지 중 무엇을 강화하는지 먼저 밝힐 것.** 해당 없으면 만들지 않는다.

---

## 절대 규칙

- 주문 상태 변경은 **오직 `lib/order-graph.ts`의 `transition()`을 통해서만** 한다. 다른 곳에서 `UPDATE ORDERS SET STATUS` 를 직접 실행하지 않는다.
- 재고 증감은 **오직 PL/SQL 프로시저를 통해서만** 한다. 애플리케이션 코드에서 재고 컬럼을 직접 계산해 넣지 않는다.
- 모든 상태 변경은 감사 로그를 남긴다. 기록 없는 변경은 허용하지 않는다.
- 권한 검사는 화면과 서버 **양쪽 모두**에서 한다. 버튼을 숨기는 것만으로 처리하지 않는다.
- 하네스 코드는 `harness/` 밖으로 나가지 않는다. 앱 코드에 검증용 코드를 섞지 않는다.
- 실제 인물의 이름·주소·연락처를 데이터로 쓰지 않는다. 전부 생성기로 만든 값만 사용한다.

---

## 도메인 용어

| 용어 | 뜻 |
|---|---|
| SKU | 상품 최소 구분 단위 |
| 로케이션 | 창고 내 위치 (`A-01-03-2` = 구역-통로-선반-단) |
| 할당(Allocation) | 주문 몫으로 재고를 찜해두는 것. 실제로 빠지진 않음 |
| 가용 재고 | `QTY_ON_HAND − QTY_ALLOCATED`. 주문 가능한 진짜 수량 |
| 피킹 | 창고를 돌며 상품을 꺼내오는 작업 |
| 웨이브 | 여러 주문의 피킹을 한 묶음으로 만든 것 |
| 패킹 | 검수 후 포장하고 송장을 붙이는 작업 |
| 출고 | 택배사에 넘기는 시점. **이때 재고가 실제 차감된다** |

주석·변수명·화면 문구에서 위 용어를 일관되게 쓴다. 임의로 바꾸지 않는다.

---

## 상태 전이

```
RECEIVED → ALLOCATED → PICKING → PACKED → SHIPPED
RECEIVED → BACKORDER → ALLOCATED
어느 단계든 → HOLD / CANCELLED  (단, SHIPPED 이후 취소 불가)
```

- 전이 정의는 `lib/order-graph.ts` 한 곳에만 존재한다.
- 서버 전이 함수, 화면의 액션 버튼, README 다이어그램 모두 이 정의에서 파생시킨다.
- 정의에 없는 전이는 거부하고 사유를 남긴다.

### 전이에 따라붙는 훅

| 훅 | 실행 시점 |
|---|---|
| `audit` (감사 로그) | 트랜잭션 **안** |
| `inventoryTx` (재고 이력) | 트랜잭션 **안** |
| `emit` (실시간 이벤트 발행) | 커밋 **이후** |

이벤트 발행을 트랜잭션 안에서 하지 않는다. 롤백됐는데 알림만 나가는 상황을 막기 위함이다.

---

## 기술 스택 (고정)

| 영역 | 사용 | 금지 |
|---|---|---|
| 프레임워크 | Next.js 15 App Router | |
| 언어 | TypeScript strict | `any` 남용 |
| DB | Oracle Database | **Prisma 사용 금지 (오라클 미지원)** |
| DB 드라이버 | node-oracledb Thin 모드 | Instant Client 의존 코드 |
| 인증 | Auth.js (Credentials) + 자체 사용자 테이블 | |
| 테이블 | TanStack Table + TanStack Virtual | 자체 구현 |
| 서버 상태 | TanStack Query | |
| 스타일 | Tailwind CSS | 인라인 style 남용 |
| 검증 | Zod | |
| 배포 | 상시 구동형 호스팅 | **Vercel (서버리스라 커넥션 풀 유지 불가)** |

새 라이브러리를 추가하기 전에 반드시 이유를 먼저 설명한다.

---

## 오라클 규칙 (요약)

- 테이블·컬럼명은 대문자 스네이크 (`ORDER_ITEMS`, `QTY_ON_HAND`)
- 문자열은 `VARCHAR2(n CHAR)`, 날짜는 `TIMESTAMP WITH TIME ZONE`
- **바인드 변수만 사용한다.** 문자열을 이어붙여 SQL을 만들지 않는다
- 여러 행을 잠글 때는 항상 `PRODUCT_ID` 오름차순으로 잠근다 (교착 상태 방지)
- `FOR UPDATE`에는 대기 시간을 건다 (`WAIT 3`)
- 커넥션 풀은 앱 시작 시 1회만 생성하고 전역에 캐싱한다

상세는 `.cursor/rules/oracle.mdc` 및 `docs/기획서.md` 6장 참조.

---

## 접근성 (모든 화면 필수)

- 시맨틱 태그를 먼저 쓴다. `div`에 클릭 핸들러를 붙이지 않는다
- 모든 폼 요소에 레이블을 연결한다 (`label for` / `aria-label`)
- 마우스 없이 키보드만으로 모든 기능을 수행할 수 있어야 한다
- 포커스 표시를 제거하지 않는다 (`outline: none` 단독 사용 금지)
- 표는 `<table>` + `<th scope>`, 정렬 상태는 `aria-sort`로 알린다
- **색만으로 정보를 전달하지 않는다.** 텍스트나 아이콘을 함께 쓴다
- 가상 스크롤 구간에서 포커스가 사라지지 않게 처리한다
- 상태 변화(저장 완료, 오류)는 스크린리더에도 전달한다 (`aria-live`)

## 반응형·호환성

- 폭 375px에서 레이아웃이 깨지지 않아야 한다
- 현장 모바일 화면은 터치 영역 최소 48×48px, 한 화면에 한 작업
- 안드로이드 크롬 / iOS 사파리 / 인앱 브라우저(카카오·네이버)에서 동작을 전제로 작성한다
- iOS 사파리의 파일 다운로드 제약, 인앱 브라우저의 카메라 권한 제약을 고려한다

---

## 화면 작성 규칙

모든 화면은 아래를 갖춰야 완료로 본다.

- 데이터 없음 / 불러오는 중 / 오류 상태 화면이 각각 존재
- 목록 화면의 필터·정렬·페이지는 URL 쿼리에 반영 (새로고침·공유 시 유지)
- 검색 입력은 디바운스 300ms
- 긴 작업은 진행률을 보여준다
- 오류 메시지는 원인과 다음 행동을 함께 알려준다 ("실패했습니다"만 쓰지 않는다)

---

## 하네스

검증 도구는 `harness/`에 독립 스크립트로 둔다.

| 명령 | 하는 일 |
|---|---|
| `harness:seed` | 상품 5천 / 로케이션 2천 / 주문 10만 / 주문상품 25만 생성 |
| `harness:concurrency` | 동시 할당 요청 발사 후 결과 검증 |
| `harness:perf` | 렌더·쿼리 성능 측정 |
| `harness:offline` | 오프라인 큐 동기화 검증 |
| `harness:invariants` | 데이터 정합성 8개 조건 검사 |
| `harness:report` | 결과를 README 성과 지표 표에 반영 |

- 기능을 먼저 만들고 하네스를 나중에 붙이지 않는다. **하네스가 먼저다.**
- 모든 하네스는 실패 시 종료 코드 1을 반환한다
- 실행 결과는 `harness/results/`에 JSON으로 남기고 커밋한다

---

## 작업 방식

- 큰 변경 전에 무엇을 바꿀지 먼저 요약하고 확인을 받는다
- 파일을 새로 만들기 전에 기존 파일에 들어갈 자리가 없는지 먼저 확인한다
- 커밋 전: 타입 체크 → 린트 → `harness:invariants` 통과
- 커밋 메시지는 한국어로, `무엇을 왜` 형식
- 추측으로 답하지 않는다. 확인이 필요하면 파일을 먼저 읽는다
- 성능·정합성 관련 수정에는 측정 결과를 함께 제시한다

---

## 참고 문서

- `docs/기획서.md` — 전체 설계 (범위, 화면, 데이터 모델, 검증 기준)
- `docs/기획서.md` 8장 — 하네스·상태 그래프·루프·훅 설계
- `docs/기획서.md` 8-5절 — 검증 기준과 정합성 조건 8개
